Make smart loading/dependences of sub-ThaTeX modules.

Switch to LuaTeX.

Reimplement double/triplecolumns because eplain's doesn't play well with lists.
(But maybe it does with ThaTeX's lists.)

Support altlabels for a chap/sec/parag.

thatexproof currently ignores the inference line style.

